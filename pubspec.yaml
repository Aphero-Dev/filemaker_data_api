name: filemaker_data_api
description: A typed Dart client for the Claris FileMaker Data API. Handles auth, token lifecycle, records, find, scripts and metadata.
version: 0.1.1
repository: https://github.com/Aphero-Dev/filemaker_data_api
issue_tracker: https://github.com/Aphero-Dev/filemaker_data_api/issues
topics:
  - filemaker
  - claris
  - rest
  - api
  - database

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.2.0
  meta: ^1.12.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
